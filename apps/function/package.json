{
  "name": "foodfast-orders-function",
  "version": "1.0.0",
  "description": "Blob-trigger (Event Grid source) function that logs the order amount when an order JSON lands in the orders container",
  "main": "src/functions/*.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.5.1"
  }
}
